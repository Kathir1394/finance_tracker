import 'package:flutter/material.dart';
class GoalForm extends StatelessWidget {
  const GoalForm({super.key});
  @override
  Widget build(BuildContext context) => const Padding(padding: EdgeInsets.all(16), child: Center(child: Text("Goal Form Placeholder")));
}
