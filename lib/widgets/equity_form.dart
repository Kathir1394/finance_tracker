import 'package:flutter/material.dart';
class EquityForm extends StatelessWidget {
  const EquityForm({super.key});
  @override
  Widget build(BuildContext context) => const Padding(padding: EdgeInsets.all(16), child: Center(child: Text("Equity Form Placeholder")));
}